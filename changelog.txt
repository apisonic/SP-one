v1.0.6
+ added "Note On" PlayMode, PlayMode can be changed from pad's right-click menu
+ added Release knob for pad's Volume envelope, enabled in "Note On" PlayMode
+ added save kit button, enabled if kit was changed
+ added alt/option + left (right) click on a Pad will mute (solo) selected pads
+ added option to open sample file in the external audio editor
+ added button to reload sample file
+ added button to load random sample file from current folder
# improved moving/adding the layer(s)
# fixed gui colors if multiple instances of a plugin was loaded

v1.0.5
+ added ctrl/command + click on a layout button ( A / B / AB ) selects all pads from that layout
+ added MIDI-IN LED indicator in the header
# improved multi-selection copy/swap
# improved Speedrum Lite drum kit import
# improved Poise drum kit import
# fixed "Select Pad With MIDI Note" behavior

v1.0.4
+ added delete layer(s) with shift + click on a layer button (number)
# improved layer sample audio preview
# fixed cutting the pads with different layer modes

v1.0.3
+ added DEMO indication
# fixed random crash during layer operations
# minor gui improvements

v1.0.2
# fixed some licensing issues

v1.0.1
# fixed crash on pad drag&drop for certain layer play modes
# fixed ring mod behaviour

v1.0.0
initial version